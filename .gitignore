node_modules

# Next.js build output — regenerated by `next build`/`next lint`, never tracked.
# The .d.ts files under src/ ARE tracked (SETUP.md:42 interface stubs); only
# .next/types and .next/cache are build artifacts.
.next/

# codegraph knowledge graph index
.codegraph/
